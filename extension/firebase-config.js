// Firebase project configuration, loaded by background.js.
// REPLACE WITH YOUR FIREBASE PROJECT CONFIG.
//
// Find these values in the Firebase console: Project settings -> General ->
// Your apps -> Web app -> SDK setup and configuration.
//
// Leaving apiKey empty runs the extension in local-only mode: heuristics
// and any previously-cached blocklist keep working, but blocklist sync and
// "Report this site as scam" are disabled until this is filled in.
const firebaseConfig = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: ''
};
